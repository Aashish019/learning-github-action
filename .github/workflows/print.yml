name: Node.js CI

on: [push]

jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Greeting
              uses: ./.github/workflows/reusable.yml
            #   with:
            #       test-input-variable: "Aashish"
