name: Node.js CI

on: [push, workflow_dispatch]

jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4
            - name: Greeting
              uses: ./.github/workflows/resuable.yml
            #   with:
            #       test-input-variable: "Aashish"
